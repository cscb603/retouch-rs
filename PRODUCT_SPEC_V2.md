# 智能照片后期校正与调色系统 — 需求规范 v2（分析与升级版）

> 基于原始 7 条改善意见，结合 retouch-rs 当前代码现状重新梳理。  
> 原 7 条定位为"产品方向意见"，本版在 **保留合理项、改造隐患项、补入缺失项** 后形成可落地的规范。  
> 状态基准日：2026-07-18。

---

## 0. 最关键澄清：「智能」到底指什么（必须先对齐，否则验收会错位）

原始需求反复出现"智能""自然智能"，但当前 retouch-rs 实际是 **参数化 + 感知映射 + 规则自动**，并非内容感知 AI。二者边界必须写清：

- **已具备的「智能」（智能默认 / 已落地）**
  - 感知映射滑块（`CurveKind`：Linear / SoftKnee / LogSat）：跟手、符合人眼，拉色不出现数码线性感
  - 规则自动：过曝时自动套 AgX 肩压缩（高光回拢）；GUI 去假色常开
  - 意图级控制：肤色「去黄 / 减淡 / 加红 / 加粉」直接用小白语言，后台解析为 OKLCH 目标
- **待建的「智能」（内容感知 / 最大缺口）**
  - 图像统计诊断：均值/标准差 L、平均彩度、肤色像素占比、动态范围、高光/暗部削波%、主色偏
  - 视觉模型辅助：场景识别 → 推荐预设（可复用 darktable 项目已有的 qwen-vl 轻量方案）
  - 输出「建议起调参数 + 置信度」，人仍可微调

**v2 定位建议：以「智能默认」为基座，把「内容感知自动」作为可选增强层**，不要把它写成不可让步的硬需求，否则会与当前实现严重错位。

---

## 1. 七条原意见逐条处置

| # | 原意见                 | 代码现状                                                  | 风险 / 隐患                    | v2 处置                                  |
| - | ------------------- | ----------------------------------------------------- | -------------------------- | -------------------------------------- |
| 1 | 色彩校正（修偏色 / 滤镜过浓失真）  | ✅ 白平衡(OKLCH) + 去假色(defake：天空/肤色限色 + 彩度衰减) + 色域软裁切     | CLI `defake` 默认关，与 GUI 不一致 | **保留并强化**：补「色偏量化诊断条」；统一 CLI/GUI 去假色默认开 |
| 2 | 光比 / 动态范围（非线性光影）    | ✅ AgX / Filmic 影调映射 + 多尺度分区融合 + 暗部/黑位双抬升(自然 roll-off) | 影调映射在线性域做，会连带压高光饱和         | **保留**：补「高光恢复」显性控制；标注影调↔色彩耦合处          |
| 3 | 皮肤处理（磨皮保质感 / 美肤色）   | ✅ FreqSepSkin(频率分离 + YCbCr 肤色域限定，发丝/眼零触碰) + 意图级控制     | 高级修图尚未接 GUI 一键             | **保留并强化**：接 GUI 一键预设；明示「只影响肤色色域」开关     |
| 4 | 几何校正（透视 + 直观裁剪）     | ✅ 透视纠正 + 直观「上下左右」边缘裁 + 宽高比按钮                          | 缺自由旋转裁切约束线                 | **保留**：后续加旋转裁切                         |
| 5 | 参数控制（滑竿自适应 / 避免线性感） | ✅ 感知映射 + 色彩条 + 白话提示 + 效果值读数                           | —                          | **保留并强化**：补超饱和/固有色护栏提示                 |
| 6 | 分离调整（光影 / 色彩独立）     | ✅ OKLCH 中 L(影调) 与 C/H(色彩) 天然解耦                        | 影调映射阶段会连带影响饱和度，分离不纯        | **保留并明确**：补「零交叉污染」保证 + 分离度自检           |
| 7 | 色域安全（评估偏差 / 损失）     | ✅ 软裁切(降 C 不截断 L/H，默认开)                                | 静默裁剪，用户不知损失多少              | **保留并升级**：补「色域偏差量化报告」(削波% / 最大 ΔC)     |

> 结论：**7 条的核心算法当前均已落地**。剩余工作集中在四类收尾——智能化、量化、GUI 化、一致性。

---

## 2. 原 7 条未覆盖、但系统必需的补入项

- **前后对比 / 缩放交互**（已建 GUI：`\` 看原图、分屏拖动线、滚轮缩放 + 拖拽平移）
- **预设可移植 / 批量**（已建 CLI + TOML 往返无损 `preset_roundtrip_lossless`）
- **色域偏差可量化报告**（待建）— 对应第 7 条「评估损失」
- **内容感知自动校正**（待建）— 对应「智能」真义，**最大缺口**
- **摄影比赛 / 行业标准内化**（待建）— 评分面板(肤 45% / 曝 24% / 调 24%) + 自动建议
- **感知保真度客观度量**（待建）— ΔE / Lab 指标，验证「自然准确」

---

## 3. 风险与隐患清单（必须正视）

1. **「智能」语义错位**：用户以为是 AI 自动修图，实际是参数化。→ 本规范第 0 节已厘清，验收以此为准。
2. **GUI 无显示环境无法实测交互手感**：本轮只在无头环境做编译 + CLI 端到端验证，缩放/分屏/色彩条/边缘裁剪需本地实测。
3. **CLI 去假色默认关**：`Adjustments::default()` 中 `defake.enabled = false`；GUI 用 `photo_default()` 才开。→ 建议 CLI 也默认开（待确认）。
4. **影调 ↔ 色彩耦合**：tone mapping / filmic 在线性域执行，高光会被压饱和，严格说「分离」不纯。→ 在 OKLCH 内对高光做 chroma 补偿。
5. **色域静默损失**：软裁切不报告裁剪量。→ 补量化报告（第 7 条升级）。

---

## 4. 后续计划（分阶段）

- **A 收尾验证（进行中）**：本地实测 GUI 交互；确认 CLI 去假色默认；可选加旋转裁切。
- **B AI 原生接口（已启动，核心升级）** ← 见 [`AI_INTERFACE.md`](./AI_INTERFACE.md)
  - 关键决策：**吸收 `retouch_app`（darktable 版）的闭环方法论，但引擎换成我们自研的**（不依赖 darktable/LibRaw，纯 Rust + OKLCH）。流程对、引擎错 → 重做引擎。
  - 已落地（2026-07-18）：`analyze`(OKLCH 量化指标 JSON) / `schema`(参数 schema 自动生成 JSON) / `render --params`(AI 写 JSON 参数，护栏 clamp) / `auto --mode local`(零 key 自治闭环 + 指标护栏 + 甜点搜索)。
  - 待建（B5）：`auto --mode api` 接入外部大模型（DeepSeek 文本决策 + qwen 视觉点评，复用 retouch_app 已验证的最便宜组合）。
- **C 色域安全可量化（第 7 条升级）**：削波% / 最大 ΔC 报告 + GUI 安全预览叠层。
- **D 行业标准内化**：评分面板 + 自动建议（肤 / 曝 / 调权重）。
- **E 高级修图 GUI 化**：FreqSepSkin / 金字塔融合 / 高光恢复接 GUI 一键预设。

> **关于「CLI 太麻烦没人用」**：普通用户确实不该碰 CLI。CLI 在这里的定位是**机器接口**——`analyze/schema/render --params/auto` 全部输出/输入结构化 JSON，是给 AI（或脚本）调用的。人类走 GUI；AI 走 CLI/HTTP。两者共用同一套引擎与参数注册表。

---

## 5. 进度看板（2026-07-18）

| 原意见         | 算法落地   | 待办收尾                | 阶段          |
| ----------- | ------ | ------------------- | ----------- |
| 1 色彩校正      | ✅      | CLI 默认统一 + 色偏诊断条    | A           |
| 2 光比/动态范围曲线 | ✅      | 高光恢复显性控制            | A/B         |
| 3 皮肤处理      | ✅      | GUI 一键接入            | E           |
| 4 几何校正      | ✅      | 旋转裁切                | A           |
| 5 参数控制      | ✅      | 护栏提示强化              | A           |
| 6 分离调整      | ✅(架构)  | 零交叉污染保证             | C           |
| 7 色域安全      | ✅(软裁切) | 量化报告                | C           |
| — 内容感知智能    | ❌      | analyze.rs + 可选视觉模型 | **B（最大缺口）** |
