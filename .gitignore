# Rust
target/
Cargo.lock
*.rlib
*.a
*.dylib
*.so
*.dll

# 皮肤精修模型与权重（红线：绝不进仓库，由打包脚本塞入 .app）
*.onnx
*.onnx.data
models/
vendor/onnxruntime/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build artifacts
**/*.rs.bk

# Search index cache (sts-x / tantivy)
.stsx-index/

# Workspace private memory (do NOT publish)
.workbuddy/

# Editor / backup
*.bak
*.rs.bak

# 嵌套的独立子项目（各自有仓库，不属于 retouch-rs，不应进本仓库）
ImageColorProfiler/
fast-video-compress/

# 打包产物（大体积二进制/zip，仅本地分发用，不进仓库）
dist/

# Secrets — 绝不提交 Key / Token（qwen_key 在 ~/.retouch，不应进仓库）
qwen_key
.env
.env.*
*.pem
